# julestest 项目规范

## 完整性验收（硬性要求）

**任何 bug 修复 / 新需求必须 e2e 验证完整实现**，不允许"代码改了就算完事"。

### 验收口径

- 用户报告的每个 bug:**重启网关 + 重启 web + 真实流量复现路径**,直接观察到现象消失。
- 新功能:**真实用户路径走通**(不是 unit test),含边界(空数据/网关掉线/重启状态保留)。
- 不能用"看代码逻辑应该对"代替验证。任何 "我觉得"/"应该"/"理论上" 都不算 done。

### 强制规定

1. **每个修复必须有可重复运行的 e2e 测试**,放 `web/scripts/_e2e_*.mjs`(或 smoke.mjs)。
2. **测试必须包含失败信号**:能区分"修复生效"vs"修复没生效"。光验证"代码跑通"不算。
3. **跨重启场景**:任何涉及持久化的 bug 必须 **kill -9 网关 → 重启 → 验证状态保留**。不能只测同进程内。
4. **screenshot 类 UI 验证**:必须用 `web/scripts/smoke.mjs` 或类似无头脚本,留下 PNG 证据;不要只用文字描述。
5. **测试脚本本身要可重复运行**:同一测试连跑两次都该通过(警惕 stateful side effects)。

### 反例(不能这么做)

- "我改了代码,tsc 通过,提交。" → ❌ 缺 e2e
- "我跑了一次,看起来对,可以了。" → ❌ 没确认可重复
- "重启网关相关代码改了,理论上重启后应该 ok。" → ❌ 必须真重启验证
- "用户报了 3 个问题,我重点修了 1 个最严重的,其它后续。" → ❌ 三个都要 e2e 验证或者明确告知"暂未实现"

### 部署/重启

`redeploy = build + restart run.sh detached via perl setsid` — 见 [julestest-deploy-restart memory](memory)。
- 改 Python(ydcore/) 必须 kill -9 网关进程再启,否则旧解释器在跑老代码。
- 改 Web(web/src/) 必须 `npm run build` + 重启 next.start,否则伺服旧 .next。
- 改完后跑 `web/scripts/_e2e_*.mjs` 之类脚本对每条 bug/需求做断言。
