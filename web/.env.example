# 复制为 .env（Prisma 读取 .env，不读 .env.local）
# SQLite 数据库文件路径（建议与其它持久化数据同目录）
DATABASE_URL="file:/Users/<you>/.youdao_course/app.db"
# Python 有道网关地址（仅 localhost；可省略，默认 http://127.0.0.1:8808）
GATEWAY_ORIGIN="http://127.0.0.1:8808"
